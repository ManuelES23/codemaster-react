import * as React from 'react';

/**
 * Hero — from codemaster-react@0.0.0.
 */
export interface HeroProps {
/** Hero takes no props — its copy and CTAs are hardcoded. */
}

export declare const Hero: React.ComponentType<HeroProps>;
