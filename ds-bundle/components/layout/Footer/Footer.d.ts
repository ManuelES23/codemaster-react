import * as React from 'react';

/**
 * Footer — from codemaster-react@0.0.0.
 */
export interface FooterProps {
/** Footer takes no props — its links are hardcoded. */
}

export declare const Footer: React.ComponentType<FooterProps>;
